<Window x:Class="TokenTrackerWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:TokenTrackerWidget.Converters"
        xmlns:vm="clr-namespace:TokenTrackerWidget.ViewModels"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="OpenCode Token Tracker"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=vm:WidgetViewModel}">
    <Window.Resources>
        <SolidColorBrush x:Key="MicaBg" Color="#20201F"/>
        <SolidColorBrush x:Key="MicaStroke" Color="#4A4A4A"/>
        <SolidColorBrush x:Key="OnSurface" Color="#E5E2E1"/>
        <SolidColorBrush x:Key="OnSurfaceVariant" Color="#C0C7D4"/>
        <SolidColorBrush x:Key="Secondary" Color="#61DBB4"/>
        <SolidColorBrush x:Key="RowHover" Color="#2A2A2A"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:BoolToDoubleConverter x:Key="ExpandAngle" TrueValue="180" FalseValue="0"/>
        <conv:BoolToBrushConverter x:Key="AmountHighlightBrush" TrueBrush="{StaticResource Secondary}" FalseBrush="{StaticResource OnSurface}"/>
        <conv:BoolToBrushConverter x:Key="ModelCostHighlightBrush" TrueBrush="{StaticResource Secondary}" FalseBrush="{StaticResource OnSurfaceVariant}"/>
        <CubicEase x:Key="EaseOut" EasingMode="EaseOut"/>
        <FontFamily x:Key="UiFont">Segoe UI Variable, Segoe UI, Inter</FontFamily>
        <FontFamily x:Key="MonoFont">Cascadia Mono, Cascadia Code, Consolas</FontFamily>
        <Style TargetType="Border">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceVariant}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource RowHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource OnSurface}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8"
            Background="{StaticResource MicaBg}"
            BorderBrush="{StaticResource MicaStroke}"
            BorderThickness="1"
            MouseLeftButtonDown="OnCardMouseLeftButtonDown"
            MouseRightButtonUp="OnCardMouseRightButtonUp">
        <Border.ContextMenu>
            <ContextMenu x:Name="CardMenu"/>
        </Border.ContextMenu>
        <StackPanel Margin="20,12">

                <!-- Primary Spend Card -->
                <TextBlock Text="{Binding TodayCostText}" FontFamily="{StaticResource UiFont}" FontSize="32" FontWeight="Bold"
                           Foreground="{Binding IsTodayCostHighlighted, Converter={StaticResource AmountHighlightBrush}}"
                           HorizontalAlignment="Center"/>

                <!-- Breakdown List -->
                <Border x:Name="BreakdownContainer" ClipToBounds="True" Visibility="Collapsed" MaxHeight="0" Opacity="0" Padding="0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBreakdownExpanded}" Value="True">
                                     <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <DoubleAnimation Storyboard.TargetProperty="MaxHeight" From="0" To="300" Duration="0:0:0.3" EasingFunction="{StaticResource EaseOut}"/>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25" EasingFunction="{StaticResource EaseOut}"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                     <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="MaxHeight" To="0" Duration="0:0:0.25" EasingFunction="{StaticResource EaseOut}"/>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" EasingFunction="{StaticResource EaseOut}"/>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0.25" Value="{x:Static Visibility.Collapsed}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <ItemsControl ItemsSource="{Binding ModelRows}" Margin="0,10,0,0" Visibility="{Binding IsBreakdownExpanded, Converter={StaticResource BoolToVis}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:ModelRowViewModel}">
                                <Border CornerRadius="8" Padding="8,6" Margin="0,1"
                                        Background="Transparent">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource RowHover}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Header}" FontFamily="{StaticResource UiFont}" FontSize="12" FontWeight="Medium"
                                                   Foreground="{StaticResource OnSurface}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding CostText}" FontFamily="{StaticResource MonoFont}" FontSize="11" FontWeight="Medium"
                                                   Foreground="{Binding IsCostHighlighted, Converter={StaticResource ModelCostHighlightBrush}}" VerticalAlignment="Bottom"
                                                   HorizontalAlignment="Right" Margin="10,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <!-- Usage Details Toggle -->
                <Button Margin="0,4,0,0"
                        HorizontalAlignment="Center"
                        Style="{StaticResource GhostBtn}"
                        Command="{Binding ToggleBreakdownCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding BreakdownToggleText}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <Path Width="8" Height="8" Stretch="Uniform"
                              Data="M19 9l-7 7-7-7"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="2.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                              Fill="Transparent"
                              RenderTransformOrigin="0.5,0.5"
                              VerticalAlignment="Center">
                            <Path.RenderTransform>
                                <RotateTransform Angle="{Binding IsBreakdownExpanded, Converter={StaticResource ExpandAngle}}"/>
                            </Path.RenderTransform>
                        </Path>
                    </StackPanel>
                </Button>

                <!-- Retry indicator -->
                <StackPanel Margin="0,8,0,0" Orientation="Horizontal"
                            Visibility="{Binding IsRetrying, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding LastErrorText}" FontFamily="{StaticResource MonoFont}" FontSize="11"
                               Foreground="#E0B341"/>
                </StackPanel>
            </StackPanel>
    </Border>
</Window>